Demo of Azure Data Factory consisting of 2 pipelines:
1. Load apache logs to SQL DB (Staging)
2. SQL DB (Staging) -> SQL DB (Fact) using a basic stored proc

Deploy either via ARM using deploy.ps1 or manually
